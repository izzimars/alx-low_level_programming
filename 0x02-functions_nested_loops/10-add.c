#include "main.h"
#include <stdio.h>

/**
 * _islower- Entry point
 * Description: 'the program's description'
 * @c: First operand
 *
 * Return: Always 0 (Success)
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return(sum);
}
