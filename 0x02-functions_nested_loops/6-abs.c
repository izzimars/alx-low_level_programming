#include "main.h"

/**
 * _abs- Entry point
 * Description: 'the program's description'
 * @n: First operand
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-1 * n);
}
